package tech.project.schedule.model.enums;

/**
 * Enumeration representing the possible status states of a project.
 * Used to track the lifecycle stage of projects in the scheduling system.
 */
public enum ProjectStatus {
    COMPLETED, // Project has been finished
    IN_PROGRESS, // Project is currently being worked on
    PLANNED, // Project is scheduled but not yet started
}
